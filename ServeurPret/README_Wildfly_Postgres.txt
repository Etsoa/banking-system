# WildFly DataSource configuration for Postgres
# Add this to standalone.xml or configure via WildFly admin console
# Example CLI command:
# /subsystem=datasources/data-source=PostgresDS:add(driver-name=postgresql,connection-url=jdbc:postgresql://localhost:5432/serveurpret_db,user-name=postgres,password=your_password)
